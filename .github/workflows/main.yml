name: Example GitHub Action

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 22£3001272@ds.study.iitm.ac.in
        run: echo "Hello, world!"
